# CLAUDE.md

TDD で対応

まずは main.go に全部まとめて実装する
分割（field / curve / sign）は中身が分かってきてから後でやる
理由: 複雑さを減らすため、まず1ファイルで動かして構造が見えてから分ける

当面のゴールは TestAgainstStdEd25519 を通すこと（Ed25519 コア完成の定義）

進捗は TODO.md でチェック管理する（項目を終えるたびに [x] を入れてコミットする）
着手中の親タスクに TODO.md で WIP を付ける（今どこをやっているか一目で分かるように）

## TODO.md の書き方（合意事項・蒸し返さない）
- 親タスク = 到達する「状態（できること）」を一文で書く。番号やラベル（field 等）は付けない
- 子タスク = その手順（関数）。前提として必要になるもの（p など）は独立させず、要る関数に畳む
- 「テストが緑」は状態の説明ではなく確認方法。親には状態を書く
- 親タスク 1 つ ≒ 1 PR

## 方針を答えるときの型（合意事項）
- 複数案を出す（一案だけにしない）
- 各案に推奨度を星で付ける（最大 ★★★★★）
- 各案に理由を添える
