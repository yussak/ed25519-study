# CLAUDE.md — このプロジェクトでの進め方（合意メモ）

Ed25519 (RFC 8032) を Go でゼロから自作して理解する**学習プロジェクト**。
ここには「決まった方針」を書き足していく。迷ったらここを見る。

## 学習モード（最重要）
- **コードはユーザー本人が書く。** Claude は雛形・ヒント・テスト例・確認方法を出す係。
  実装（field.go / curve.go / sign.go の中身）の答えを先に書かない。
- 詰まったら「答え」ではなく「次の一歩」と「確認方法」を出す。
- テストは学びの一部。最初の1本だけ worked example を示し、以降はユーザーが真似て書く。

## 進め方：TDD の最小ループ
1関数ずつ **Red → Green → Refactor** を回す。
1. 🔴 Red: 小さな失敗テストを1本書く（`go test` が落ちるのを確認）
2. 🟢 Green: 通る**最小限**のコードを書く
3. ♻️ Refactor: 挙動を変えずに整える

- テストは「手で正しさを確認できる小さなケース」から。巨大な `p` は
  `(p-1)+3 ≡ 2 (mod p)` のような等式で検証する（桁を知らなくてよい）。
- 逆元などは property test（`feMul(a, feInv(a)) == 1`）で確認する。

## 実装順序と各段のゴール
1. **field.go** 有限体 GF(p), p=2^255-19 — 成功: `feMul(a, feInv(a)) == 1`
2. **curve.go** ねじれEdwards曲線 — 成功: `onCurve(B)` かつ `add(B,B)==scalarMul(2,B)`
3. **sign.go** 鍵生成/署名/検証 — 成功: 自作鍵で sign→verify が true、1bit変えるとfalse、
   最後に RFC 8032 テストベクタ / 標準 `crypto/ed25519` と一致

## 技術的な決めごと
- 標準ライブラリのみ。大きな整数は `math/big`、ハッシュは `crypto/sha512`。
- モジュール: `ed25519-study/`（`go.mod` あり）。
- タスク管理は `ed25519-study/TODO.md`。

## 確認コマンド
```
cd ed25519-study
go run .          # main が動く（実装前でもここは緑を保つ）
go test ./...     # TDD のループはここで回す
```

## 決定ログ（決めたら追記する）
- 2026-07-29: 学習モード（Claudeは答えを書かない）+ TDD 最小ループで進める、と合意。
- 2026-07-29: 実装順序 field → curve → sign。標準ライブラリのみ使用。
- （例）座標系: アフィン or 拡張座標 → **未決**（curve 着手時に判断）
